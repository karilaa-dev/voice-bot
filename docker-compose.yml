services:
  voice-bot:
    build: .
    volumes:
      - ./data:/app/data
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - ADMIN_ID=${ADMIN_ID}
      - COMMAND_PREFIX=${COMMAND_PREFIX:-.}
      - DB_PATH=/app/data/voice.db
    restart: unless-stopped
